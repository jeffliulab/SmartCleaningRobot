# SmartCleaningRobot — 任务报告模板

> 使用本模板整理任何任务（T1-T7）的训练报告。将 `<占位符>` 替换为实际内容。

---

## 封面信息

- **报告标题：** `<任务名称>` 强化学习训练报告
- **任务编号：** `<T1-T7>`
- **Gym ID：** `<SmartCleaningRobot-XXX-v0>`
- **作者：** `<姓名>`
- **日期：** `<YYYY-MM-DD>`

---

## 1. 任务概述

### 1.1 任务描述

> 用自己的话描述该任务的目标、场景和挑战。

`<描述任务目标：机器人需要做什么？环境是什么样的？>`

### 1.2 关键参数

| 参数 | 值 |
|------|-----|
| 观测空间维度 | `<N-dim>` |
| 动作空间维度 | `<M-dim>` |
| 机器人类型 | `<JetBot / TurtleBot4+WidowX250>` |
| Episode 时间限制 | `<X 秒>` |
| 成功条件 | `<具体条件>` |

### 1.3 观测空间分析

> 列出观测空间的每个组件及其含义。解释为什么需要这些信息。

| 组件 | 维度 | 范围 | 作用 |
|------|------|------|------|
| `<组件名>` | `<N>` | `<[a, b]>` | `<该信息帮助机器人做什么>` |

### 1.4 动作空间分析

> 列出动作空间的每个组件及其物理含义。

| 索引 | 含义 | 范围 | 实际物理量 |
|------|------|------|-----------|
| `<0>` | `<线速度>` | `<[-1,1]>` | `<×0.3 = m/s>` |

### 1.5 奖励函数分析

> 列出所有奖励组件，分析设计意图。

| 组件 | 权重 | 类型 | 设计意图 |
|------|------|------|---------|
| `<组件名>` | `<权重>` | `<密集/稀疏>` | `<为什么需要这个奖励>` |

---

## 2. 背景知识

### 2.1 算法原理

> 用自己的话解释所使用的 RL 算法（PPO / SAC）的核心思想。

**我使用的算法：** `<PPO / SAC>`

**核心思想：**
`<用 3-5 句话概括算法的核心原理>`

**关键公式：**
`<列出 1-2 个最核心的公式，并解释含义>`

### 2.2 相关技术

> 描述任务中用到的其他技术（如差分驱动、LiDAR、BFS 距离场、Coverage Grid 等）。

`<技术名>：<用自己的话描述原理和在本任务中的作用>`

---

## 3. 实验配置

### 3.1 硬件环境

| 组件 | 规格 |
|------|------|
| GPU | `<型号, VRAM>` |
| CPU | `<型号>` |
| 内存 | `<GB>` |

### 3.2 超参数配置

> 列出关键超参数及其选择理由。

| 参数 | 值 | 选择理由 |
|------|-----|---------|
| 学习率 | `<3e-4>` | `<默认值 / 调整理由>` |
| 并行环境数 | `<128>` | `<显存允许的最大值>` |
| 网络结构 | `<[256,128,64]>` | `<任务复杂度对应的容量>` |
| `<其他参数>` | `<值>` | `<理由>` |

### 3.3 训练命令

```bash
<你实际运行的训练命令>
```

---

## 4. 训练结果

### 4.1 训练曲线

> 插入 training_curves.png，并对每个子图进行分析。

**生成命令：**
```bash
python train/scripts/common/plot_training.py <your_log_dir> --smooth 10
```

![训练曲线](training_curves.png)

**曲线分析：**
- **平均奖励：** `<描述曲线走势：初始值 → 上升阶段 → 收敛值>`
- **Episode 长度：** `<描述变化趋势>`
- **成功率：** `<描述上升过程和最终值>`
- **滚动平均：** `<描述平滑后的趋势>`

### 4.2 关键指标

| 指标 | 最终值 | 目标值 | 是否达标 |
|------|--------|--------|---------|
| 平均奖励 | `<值>` | `<目标>` | `<是/否>` |
| 成功率 | `<值>` | `<目标>` | `<是/否>` |
| 平均 Episode 长度 | `<值>` | `<越低越好>` | `<是/否>` |
| 总训练步数 | `<值>` | `<计划值>` | — |
| 总训练时间 | `<值>` | — | — |

### 4.3 训练阶段分析

> 分析训练过程的不同阶段（参考 TRAINING_GUIDE.md 第 8.1 节）。

| 阶段 | 步数范围 | 奖励范围 | 行为描述 |
|------|---------|---------|---------|
| 随机探索 | `<0 - ?K>` | `<值>` | `<机器人在做什么>` |
| 初步学习 | `<? - ?K>` | `<值>` | `<学到了什么行为>` |
| 快速提升 | `<? - ?M>` | `<值>` | `<关键突破>` |
| 收敛/稳定 | `<? - ?M>` | `<值>` | `<最终行为>` |

---

## 5. 行为分析

### 5.1 回放观察

> 运行 play.py 观察训练好的策略，描述机器人的行为。

**回放命令：**
```bash
python train/scripts/common/play.py --task <TASK_ID> --num_envs 1 --video
```

**行为描述：**
`<描述机器人的行为特征：如何导航、如何避障、如何完成任务>`

### 5.2 成功案例

`<描述 1-2 个典型的成功 episode：机器人如何完成任务>`

### 5.3 失败案例

`<描述 1-2 个典型的失败 episode：机器人在什么情况下失败、为什么>`

### 5.4 学到的策略

> 分析机器人学到了哪些有意义的行为模式。

`<例如：机器人学会了沿墙走、学会了在死胡同前提前转弯、学会了先导航到目标附近再伸出手臂等>`

---

## 6. 对比实验（可选）

> 如果做了多组实验，在这里对比结果。

### 6.1 实验列表

| 实验 | 变量 | 最终奖励 | 成功率 | 训练时间 |
|------|------|---------|--------|---------|
| Baseline | 默认参数 | `<值>` | `<值>` | `<值>` |
| 实验 A | `<改了什么>` | `<值>` | `<值>` | `<值>` |
| 实验 B | `<改了什么>` | `<值>` | `<值>` | `<值>` |

### 6.2 对比分析

`<不同实验的结果差异说明了什么？>`

---

## 7. 讨论

### 7.1 主要发现

> 总结 2-3 个最重要的发现。

1. `<发现 1>`
2. `<发现 2>`
3. `<发现 3>`

### 7.2 遇到的问题及解决

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| `<问题描述>` | `<根因>` | `<如何解决>` |

### 7.3 不足与改进方向

> 诚实地分析当前结果的不足和可能的改进方向。

`<例如：成功率还未达到目标、某些场景下表现不佳、超参数未充分搜索等>`

---

## 8. 结论

> 用 3-5 句话总结整个实验的过程和结论。

`<总结：做了什么任务、用了什么方法、取得了什么结果、有什么启示>`

---

## 附录

### A. 完整超参数表

> 从 params/agent.yaml 复制完整配置。

```yaml
<粘贴 agent.yaml 内容>
```

### B. 环境配置

> 从 params/env.yaml 复制完整配置。

```yaml
<粘贴 env.yaml 内容>
```

### C. 训练日志路径

```
<你的日志目录完整路径>
```

### D. 参考资料

- [GETTING_STARTED.md](../../GETTING_STARTED.md) — 环境搭建
- [TRAINING_GUIDE.md](../../TRAINING_GUIDE.md) — 训练指南
- `<任务 FULL_GUIDE.md 链接>`
- PPO 原始论文: Schulman et al., "Proximal Policy Optimization Algorithms", 2017
- SAC 原始论文: Haarnoja et al., "Soft Actor-Critic", 2018
